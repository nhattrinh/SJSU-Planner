<div class="form-horizontal">
  <div class="col-md-6 col-md-offset-3">

<%= form_for(course_prereq) do |f| %>
  <% if course_prereq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_prereq.errors.count, "error") %> prohibited this course_prereq from being saved:</h2>

      <ul>
      <% course_prereq.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

        <div class="form-group">
            <%= f.label :course_id, class: 'col-sm-2 control-label' %>
            <div class="col-sm-10">
         <%= f.collection_select :course_id, Course.all, :id, :full_name, {prompt: true}, {class: 'form-control'} %>
            </div>
        </div>

        <div class="form-group">
    <%= f.label :prereq_id, class: 'col-sm-2 control-label' %>
          <div class="col-sm-10">
    <%= f.collection_select :prereq_id, Course.all, :id, :full_name, {prompt: true}, {class: 'form-control'} %>
        </div>
  </div>

        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
    <%= f.submit class: 'btn btn-primary' %>
        </div>
  </div>
<% end %>

  </div>
</div>