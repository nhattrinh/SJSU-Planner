<%= form_for(course_prereq) do |f| %>
  <% if course_prereq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_prereq.errors.count, "error") %> prohibited this course_prereq from being saved:</h2>

      <ul>
      <% course_prereq.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.collection_select :course_id, Course.all, :id, :full_name, prompt: true %>
  </div>

  <div class="field">
    <%= f.label :prereq_id %>
    <%= f.collection_select :prereq_id, Course.all, :id, :full_name, prompt: true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
