<p>
    <strong>Name:</strong>
    <%= @semester.name %>
</p>

<p>
    <strong>Year:</strong>
    <%= @semester.year %>
</p>

<h2>Courses</h2>
<table>
    <tr>
        <th>Subject</th>
        <th>Number</th>
        <th>Credits</th>
        <th colspan="3"></th>
    </tr>
    <%= render @semester.courses %>
</table>

<h2>Add a course:</h2>
<%= render 'courses/form' %>


<%= link_to 'Edit', edit_semester_path(@semester) %> |
<%= link_to 'Back', semesters_path %>
